# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/scheduler

# Redis connection
REDIS_HOST=localhost
REDIS_PORT=6379

# API server port
PORT=3000

# Comma-separated list of allowed origins for CORS
# Examples:
#   - Single domain: ALLOWED_ORIGINS=https://your-app.com
#   - Multiple domains: ALLOWED_ORIGINS=https://app1.com,https://app2.com
#   - Allow all origins: ALLOWED_ORIGINS=* (or leave empty)
ALLOWED_ORIGINS=

# Comma-separated list of allowed target hosts for scheduled HTTP jobs
# Examples:
#   - Exact matches: ALLOWED_TARGET_HOSTS=api1.example.com,api2.example.com
#   - Wildcard subdomains: ALLOWED_TARGET_HOSTS=*.example.com,*.another.com
#   - Mixed: ALLOWED_TARGET_HOSTS=api.example.com,*.another.com
#   - Allow all hosts: Leave empty
ALLOWED_TARGET_HOSTS=
